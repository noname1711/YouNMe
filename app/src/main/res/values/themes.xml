<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.YouNMe" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Chỉnh cho mất thanh actionbar mặc định màu tím. -->
        <item name="android:windowFullscreen">true</item>
        <!-- áp dụng vào theme trên manifest là xong -->
    </style>
    <style name="Theme.YouNMe" parent="Base.Theme.YouNMe" />
</resources>